# Photo Notes

一个纯静态的个人摄影时间线，参考 `photos.sambecker.com` 的轻量照片流形态。

## 使用

```bash
python3 -m http.server 4175
```

打开：

```text
http://127.0.0.1:4175
```

## 修改照片

照片数据在 `index.html` 底部的 `photos` 数组中。替换标题、日期、地点、相机型号和图片地址即可。

## 许可

MIT
